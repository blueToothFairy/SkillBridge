# 🚀 Kế Hoạch Phát Triển SkillBridge MVP (7 Ngày)

Kế hoạch chi tiết dành cho **Coder 1** và **Coder 2** thuộc dự án **SkillBridge MVP**, được tối ưu hóa dựa trên tài liệu kiến trúc tại [`docs/`](./docs). 

> [!IMPORTANT]
> **Nguyên tắc triển khai:**
> 1. **Hoàn thành Cài đặt & Test trong 5 ngày đầu tiên** (Day 1 - Day 5).
> 2. **Giảm tối đa sự phụ thuộc (Decoupling):** Định nghĩa API Contract và TypeScript Types độc lập trước, cho phép Coder 1 và Coder 2 làm song song các module mà không phải chờ nhau.
> 3. **Day 6:** Đóng đóng tính năng (Feature Freeze), chỉ sửa lỗi blocker & chạy Dry-run.
> 4. **Day 7:** Kiểm tra gói nộp (`.zip`), đảm bảo chạy lại từ đầu thành công.

---

## 🗓️ Bảng Kế Hoạch Chi Tiết (Day 1 – Day 7)

| Ngày | Ngày thực tế | Assignee | Hạng mục công việc (Task) | Kết quả đầu ra (Expected Output) |
| :--- | :--- | :--- | :--- | :--- |
| **Day 1** | **25/07/2026** *(Thứ Bảy)* | **Thịnh** | **Database Schema & Auth Module**<br>• Thiết lập Prisma Schema 10 bảng CSDL (`users`, `student_profiles`, `sme_profiles`,...) & chạy migration.<br>• Xây dựng Backend Auth API (Register, Login, JWT Token middleware).<br>• Xây dựng Frontend Auth Context & Trang Login/Register (`/login`, `/register`). | • `prisma/schema.prisma` hoàn chỉnh & Database migration.<br>• Endpoints: `POST /api/auth/register`, `POST /api/auth/login`.<br>• UI Login/Register & Lưu JWT token trong LocalStorage. |
| | | **Thịnh** | **Project Management & Tag System**<br>• Xây dựng API Tag & Category (`GET /api/tags`).<br>• Xây dựng API Quản lý dự án (`POST /api/projects`, `GET /api/projects`, `GET /api/projects/:id`).<br>• Xây dựng UI SME Đăng bài (`ProjectForm.tsx`) & Trang Browse bài viết công khai. | • API CRUD Projects & Tags hoàn chỉnh.<br>• Trang `/projects` (Danh sách dự án) & `/projects/create` (Đăng bài mới).<br>• Form đăng bài với budget, skill tags, category. |
| **Day 2** | **26/07/2026** *(Chủ Nhật)* | **Hà** | **Application & Skill Matching**<br>• API Sinh viên Ứng tuyển (`POST /api/applications`).<br>• Algorithm tính điểm Skill-Matching (% trùng khớp kỹ năng sinh viên vs yêu cầu dự án).<br>• UI Sinh viên nộp hồ sơ (`ApplyModal.tsx`) & Danh sách ứng viên dành cho SME (`/sme/projects/:id/applicants`). | • API `POST /api/applications` & Logic matching.<br>• UI Sinh viên nhấn "Ứng tuyển" với Cover Letter.<br>• UI SME xem danh sách ứng viên đã sắp xếp theo % Skill-Matching. |
| | | **Thịnh** | **Milestone Tracking System**<br>• API Tạo & Quản lý Cột mốc (`POST/GET /api/milestones`).<br>• API Sinh viên nộp Deliverable URL (`PATCH /api/milestones/:id/submit`).<br>• API SME Đánh giá & Duyệt mốc (`PATCH /api/milestones/:id/review`).<br>• UI Milestone Progress Bar & Trang quản lý mốc dự án (`/projects/:id/milestones`). | • API Milestones Lifecycle (Pending ➔ Submitted ➔ Approved/Revision).<br>• UI xem mốc tiến độ dự án trực quan cho cả SME và Sinh viên.<br>• Form nộp URL sản phẩm & Form SME gửi feedback. |
| **Day 3** | **27/07/2026** *(Thứ Hai)* | **Hà** | **Simulated Escrow System**<br>• API Ký quỹ mô phỏng (`POST /api/escrow/deposit`, `POST /api/escrow/release`, `GET /api/escrow/status`).<br>• Xử lý chuyển trạng thái Escrow (`PENDING` ➔ `HELD` ➔ `RELEASED`).<br>• UI SME nạp/ký quỹ kinh phí dự án (`EscrowModal.tsx`) & Hiển thị Badge trạng thái ký quỹ. | • API Ký quỹ an toàn độc lập.<br>• Luồng nạp tiền mô phỏng & Tự động khóa/giải ngân kinh phí theo cột mốc.<br>• UI hiển thị số dư & trạng thái ký quỹ rõ ràng. |
| | | **Thịnh** | **Project Acceptance & Auto-Reminder Flow**<br>• API SME Nghiệm thu toàn bộ dự án (`POST /api/projects/:id/accept`).<br>• API Yêu cầu chỉnh sửa (`POST /api/projects/:id/revise`).<br>• Logic nhắc nhở nghiệm thu tự động (`acceptance_reminders`).<br>• UI Trang xác nhận nghiệm thu dự án (`/projects/:id/accept`). | • API Nghiệm thu & Chuyển trạng thái dự án (`COMPLETED`).<br>• UI SME nghiệm thu dự án hoặc gửi yêu cầu sửa đổi.<br>• Cron script/Utility xử lý tự động duyệt sau thời gian quy định. |
| **Day 4** | **28/07/2026** *(Thứ Ba)* | **Thịnh** | **Verified Portfolio & Student Profile**<br>• API Tự động tạo Hồ sơ năng lực đã xác thực (`verified_portfolio_entries`) sau khi nghiệm thu.<br>• API Lấy danh sách Portfolio của sinh viên (`GET /api/portfolio/student/:id`).<br>• UI Trang Hồ sơ cá nhân Sinh viên (`/student/profile`) tích hợp Portfolio được xác thực bởi SME. | • API Portfolio tự động đồng bộ từ dự án thành công.<br>• Trang Profile sinh viên ấn tượng với các badge dự án đã hoàn thành và kỹ năng ứng dụng. |
| | | **Hà** | **Certificate Generator & Demo Seed Data**<br>• API Cấp Chứng nhận điện tử (`POST /api/certificates`) kèm mã xác thực duy nhất (`verification_code`).<br>• API Tra cứu chứng nhận công khai (`GET /api/certificates/verify/:code`).<br>• UI Chứng nhận điện tử (`/certificates/:code`) & Nút Export PDF/Share.<br>• Script `prisma/seed.ts` nạp dữ liệu mẫu phong phú (Users, Projects, Applications, Milestones). | • API & UI Chứng chỉ điện tử đẹp mắt có mã Verification QR/Code.<br>• File `seed.ts` hoàn chỉnh nạp sẵn 5+ dự án, 10+ sinh viên, 3+ SME giúp chạy demo mượt mà. |
| **Day 5** | **29/07/2026** *(Thứ Tư)* | **Hà** | **Integration & E2E Testing (Flow 1 & 2)**<br>• Test tích hợp luồng: Đăng ký/Đăng nhập ➔ SME Đăng dự án ➔ Sinh viên tìm kiếm & Ứng tuyển.<br>• Sửa các lỗi phát sinh về Type, API Response format, UI layout.<br> | • Luồng Đăng bài & Ứng tuyển chạy mượt từ A-Z. |
| | | **Thịnh** | **Integration & E2E Testing (Flow 3 & 4) + Documentation**<br>• Test tích hợp luồng: Ký quỹ ➔ Nộp mốc dự án ➔ Duyệt mốc ➔ Nghiệm thu ➔ Nhận Certificate & Portfolio.<br>• Cập nhật hoàn thiện `README.md` hướng dẫn chạy chi tiết.<br>• Chụp ảnh giao diện (Screenshots) & Quay video demo ngắn (GIF/MP4). | • Toàn bộ luồng nghiệp vụ end-to-end hoạt động trơn tru.<br>• Document `README.md` cập nhật kèm hình ảnh/video minh họa. |
| **Day 6** | **30/07/2026** *(Thứ Năm)* | **Coder 1 & Coder 2** | **Feature Freeze, Blocker Fixing & Dry-Run**<br>• Đóng băng tính năng mới (Không thêm tính năng).<br>• Tập trung sửa toàn bộ lỗi Blocker/Critical nếu có.<br>• Chạy thử nghiệm Kịch bản Demo Live (Dry-Run) cùng với nhóm (BP Owner & Slide Owner). | • Hệ thống ổn định 100%, không bị crash khi demo.<br>• Kịch bản demo live mượt mà, sẵn sàng trình bày. |
| **Day 7** | **31/07/2026** *(Thứ Sáu)* | **Coder 1 & Coder 2** | **Final Packaging & Quality Check**<br>• Dọn dẹp source code, xóa các file rác/temp.<br>• Đóng gói thư mục mã nguồn thành file `04.zip`.<br>• Giải nén `04.zip` sang thư mục sạch và chạy thử `npm install && npm run dev` để đảm bảo không thiếu file.<br>• Kiểm tra lại lần cuối trước khi nộp lên Moodle. | • File `04.zip` chuẩn chỉnh, sẵn sàng nộp.<br>• Đảm bảo bất kỳ ai giải nén cũng chạy được ngay lập tức. |

